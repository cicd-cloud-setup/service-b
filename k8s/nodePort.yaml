apiVersion: v1
kind: Service
metadata:
  name: service-b-nodeport
spec:
  type: NodePort
  selector:
    app: service-b # <-- Make sure this matches your pod/deployment's labels
  ports:
    - port: 3000 # Service port
      targetPort: 3000 # Pod port
